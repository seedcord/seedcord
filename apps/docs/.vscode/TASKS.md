[ ] 1. Make sure constructed signatures for types and interfaces are not all in a single line but are formatted based on length. can use prettier to format the code. This is only for the signature in the entity header, and the signatures in the entity members. Codeblocks basically.

[ ] 2. Integrate fuzzy searching in the search in docs-engine. You can use an external library for this. Make sure it still is fast and performant. Right now I'm able to get relevant results in under 5ms once the index is built. There are currently no tests in the docs-engine directory, and neither are there any in the docs-generator directory. So technically, both can end up having edge cases. Furthermore, the smoke files in both these folders are being used to actually generate files. The smoke in docs-engine is actually used to test the search functionality. The smoke in docs-generator is used to generate all the documentation jsons. So that smoke should be removed, and instead shifted to the root/scripts directory so that CI/CD can use it via pnpm. (It should be typescript). Anyway, do your research and figure out how to implement all these changes. In the tests you add, make sure to use multiple files if needed, to simulate real world usage, and to test edge cases. You can check how other packages have their tests set up in Seedcord. Also, if you make any files, make them inside the tests directory itself an clean up afterwards.

STRICTLY FOLLOW AGENTS.md
