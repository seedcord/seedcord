---
title: Components
description: Buttons, select menus, modals, the typed customId that routes them, and pagination.
---

This section is being written.

It will cover declaring a customId shape, putting one on a component, routing a button click, select menus, modals, updating the message a component came from, stale customIds, and paging both an array and a query too large to load at once.
