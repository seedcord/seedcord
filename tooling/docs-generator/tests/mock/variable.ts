/**
 * A simple mock variable for testing documentation generation.
 *
 * This variable serves as a basic export for testing purposes.
 *
 * @example
 * ```typescript
 * console.log(mockVariable); // 'mock value'
 * ```
 */
export const mockVariable = 'mock value';
