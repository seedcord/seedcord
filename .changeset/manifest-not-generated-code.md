---
'@seedcord/errors': patch
---

New `ConfigManifestNotGenerated` code, thrown when the generated route manifest is imported before `seedcord build` emits it. New `LifecycleRestartAfterFailure` code, thrown when a host whose startup failed is started again.
