---
'@seedcord/plugin-mongoose': minor
---

New `@seedcord/plugin-mongoose`, replacing the mongoose surface from `@seedcord/plugins`. It attaches to a gateway bot and to an http server bot.

Declare a model's schema as a `public static schema` member. Model cleanup covers only the models this plugin registered.
