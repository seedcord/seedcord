---
'@seedcord/gateway': minor
---

`new Seedcord({ logger })` configures logging (level, sinks, per-channel overrides). Omitted fields keep the node defaults. `@seedcord/gateway` re-exports `@seedcord/logger`.
