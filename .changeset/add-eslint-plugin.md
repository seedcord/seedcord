---
'@seedcord/eslint-plugin': minor
---

Add @seedcord/eslint-plugin, type-aware rules that catch seedcord footguns before runtime. Exports `recommended` (the seedcord rules) and a `seedcord` preset that layers them over eslint-plugin-discordjs's recommended set.
