---
'@seedcord/errors': patch
---

Adds `CoreDirectoryImportFailed`, `CoreDirectoryUnreadable`, and `InteractionRouteExportMissing`.

`InteractionDuplicateRoute` now specifies the file each colliding class came from.
