---
'@seedcord/logger': minor
---

`formatBody(record)` renders the styled message body (interpolation, args, stack) without the time, level, or label prefix, for consumers that draw their own log chrome.
