---
'@seedcord/plugins': minor
---

bump `kysely` `^0.28.9` → `^0.29.2`. migration helpers now imported from `'kysely/migration'` (root re-exports deprecated). `MigrationTarget` switched from `typeof NO_MIGRATIONS` to the `NoMigrations` interface.
