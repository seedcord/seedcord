---
'@seedcord/core': minor
---

Adds the six interaction route decorators (`@SlashRoute`, `@AutocompleteRoute`, `@ContextMenuRoute`, `@ButtonRoute`, `@ModalRoute`, `@SelectMenuRoute`), shared by both transports, each cross-checking the handler's generics at compile time.
