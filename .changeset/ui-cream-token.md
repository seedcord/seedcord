---
'@seedcord/ui': minor
---

Add a stable `--cream` brand token (the canonical cream `#f8f6e8`) and warm the dark theme ground from the cool `#070917` to a warm near-black `#0e120c`, so the brand reads warm in both light and dark across the apps.
