---
'@seedcord/plugins': minor
---

**BREAKING:** `Mongo` is now `Mongoose`, and every other `Mongo*` export is `Mongoose*`.

**BREAKING:** `KyselyPg` is now `KyselyPostgres`, and every other `Kpg*` export is `Kysely*`.

`MongooseOptions` is exported.
