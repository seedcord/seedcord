---
'@seedcord/eslint-config': minor
---

Add an opt-in `discordRules` flag that enables a set of rules that are specific to discord.js. There's only one rule right now which bans importing discord.js component builders (use `@discordjs/builders`).
