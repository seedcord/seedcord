---
'@seedcord/cli': patch
---

The dev sidebar no longer lists the config's interactions/events/subscribers paths, they duplicate the user's own config file. The banner instead shows the running instance's framework version, and the rail width now locks at the first 'running' render of each run.
