---
'seedcord': patch
---

Add a channel and level filter picker to the dev TUI, opened with f, with wrapped colored chips plus solo and toggle. A new truecolor palette keeps the TUI's own colors consistent across terminals.
