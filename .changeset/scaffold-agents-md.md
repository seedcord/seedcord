---
'create-seedcord': minor
---

A scaffolded project now carries an `AGENTS.md`, with a `CLAUDE.md` pointing at it. It sends an agent to the guide's index before it writes code, then covers the transport this project picked, the folder layout, and when to run codegen.
