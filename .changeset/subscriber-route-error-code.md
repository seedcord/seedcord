---
'@seedcord/errors': patch
---

Adds `SubscriberRouteNotASubscriber` for a manifest subscriber row whose named export does not extend `Subscriber`. `InteractionRouteExportMissing` stays for an export name the module lacks.
