---
'@seedcord/core': minor
---

`@RegisterCommand()` with no arguments deploys a command to the guilds in `commands.guilds`, and globally while that key is unset. Pass `'config'` among your own ids to reach that list and somewhere else.
