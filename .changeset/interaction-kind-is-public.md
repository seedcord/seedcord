---
'@seedcord/core': minor
'@seedcord/gateway': minor
'@seedcord/http': minor
---

`InteractionKind` now ships from the package root. Use its members to compare against the `kind` you read off `interactionDispatched`.
