---
'@seedcord/errors': minor
'@seedcord/core': patch
---

Added `CoreCommandGuildDeployFailed` (1214). A guild command deploy Discord refuses now prints that guild id, with Discord's own error kept as the cause.
