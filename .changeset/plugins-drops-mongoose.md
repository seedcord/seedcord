---
'@seedcord/plugins': minor
---

**BREAKING:** the mongoose surface moves to `@seedcord/plugin-mongoose`. Install that package and update the import path, including any `declare module` block that augments `MongooseServices`.
