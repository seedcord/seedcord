---
'@seedcord/eslint-config': minor
---

Add `registerUnicornPlugin`, `registerDiscordjsPlugin`, and `registerSeedcordPlugin`. Unicorn requires eslint 10, so set `registerUnicornPlugin: false` on eslint 9.

`proseWrap` is now `never`.
