---
'discord-component-embed': patch
---

Error messages now show what broke a rule and how to fix it, like the start of a label that runs too long or where a `<Thumbnail>` placed straight in a `<Container>` should go.
